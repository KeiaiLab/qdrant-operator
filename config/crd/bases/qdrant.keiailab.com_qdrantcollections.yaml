---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: qdrantcollections.qdrant.keiailab.com
spec:
  group: qdrant.keiailab.com
  names:
    kind: QdrantCollection
    listKind: QdrantCollectionList
    plural: qdrantcollections
    singular: qdrantcollection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.pointsCount
      name: Points
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: QdrantCollection is the Schema for the qdrantcollections API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              QdrantCollectionSpec — 같은 네임스페이스의 QdrantCluster 위 컬렉션을 선언적으로
              보장(ensure)·채택(adopt)한다. 파라미터 불일치 시 파괴적 재생성 대신 Degraded 로
              표면화한다(자동 재생성 절대 금지 — 설계 안전 원칙).
            properties:
              clusterRef:
                description: 같은 네임스페이스의 QdrantCluster 이름.
                minLength: 1
                type: string
              collectionName:
                description: Qdrant 상 컬렉션 이름. 비우면 metadata.name 사용.
                type: string
              onDelete:
                default: Retain
                description: |-
                  CR 삭제 시 컬렉션 처분 — 기본 Retain(데이터 보존). Delete 명시 시에만 파이널라이저로
                  컬렉션을 삭제한다.
                enum:
                - Retain
                - Delete
                type: string
              replicationFactor:
                default: 1
                format: int32
                minimum: 1
                type: integer
              shardNumber:
                default: 1
                description: 생성 시 고정 — 이후 변경은 re-shard 워크플로(후속 마일스톤) 대상.
                format: int32
                minimum: 1
                type: integer
              vectors:
                description: |-
                  VectorsSpec 은 컬렉션 벡터 스키마(단일 벡터). 신규 생성 시 사용하고, 기존 컬렉션
                  채택 시에는 검증 기준으로만 쓴다.
                properties:
                  distance:
                    default: Cosine
                    enum:
                    - Cosine
                    - Dot
                    - Euclid
                    - Manhattan
                    type: string
                  size:
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - size
                type: object
            required:
            - clusterRef
            - vectors
            type: object
          status:
            description: QdrantCollectionStatus — 관측 상태.
            properties:
              adopted:
                description: 채택(기존 컬렉션 인수) 여부 — true 면 이 CR 생성 이전부터 존재하던 컬렉션.
                type: boolean
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: Pending | Ready | Degraded
                type: string
              pointsCount:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
